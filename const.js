const quizInterval = 1;
const longTermQuizInterval = quizInterval * 5;
const second = 1000;
const minute = 60 * second;
const extendIntervalAfter = 3 * minute;

export { quizInterval, longTermQuizInterval, extendIntervalAfter };
